# ABRAXAS A v1.0 Brain Navigator 2.0 + Handoff Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** deliver stable v1.0 with Brain Navigator 2.0 and a complete v1.1 handoff package.

**Architecture:** Incremental layer over v0.9.6.1. Keep the recovered single end-of-file bootstrap and existing data/Production Graph. Extend Home UI state, procedural target transforms and Home-only presentation. Package continuity as explicit text/JSON artifacts.

**Tech Stack:** standalone HTML, vanilla JS, Canvas 2D, CSS, localStorage, Python build/package/audit scripts, Node runtime smoke tests.

## Global Constraints
- Offline standalone must work via file://.
- Production Graph/content_id contracts remain stable.
- Brain zoom range is exactly 1.0–2.0; 1.0 is v0.9.6.1 baseline.
- Liquid Glass is functional overlay material, not an opaque content card.
- Visual Composer remains a v1.1+ candidate, not v1.0 scope.

### Task 1: Brain Navigator 2.0
- [ ] Add failing release/runtime tests for zoom, popovers, leader lines and baseline/max range.
- [ ] Implement state + target scaling + controls.
- [ ] Implement visible hotspot nodes, tool icons, leader lines and hover/focus popovers.
- [ ] Add procedural brain guide and keep morph targets.
- [ ] Re-run runtime and release tests.

### Task 2: Liquid Glass Home overlays
- [ ] Add failing style tests for translucent brain copy/status/zoom controls.
- [ ] Implement glass overlay rules and ensure brain remains visible.
- [ ] Re-run tests.

### Task 3: v1.0 release + continuity
- [ ] Migrate version/state/build/package to 1.0 with legacy v0.9.6.1 state support.
- [ ] Write v1.1 handoff prompt, visual direction prompt, roadmap JSON and release docs.
- [ ] Build standalone and run source + standalone boot/navigation gates.

### Task 4: Quality/package
- [ ] Run full release suite.
- [ ] Export/audit/strict-scan prompts.
- [ ] Package detached.
- [ ] Verify ZIP integrity, manifests, HTML equality and hashes.
