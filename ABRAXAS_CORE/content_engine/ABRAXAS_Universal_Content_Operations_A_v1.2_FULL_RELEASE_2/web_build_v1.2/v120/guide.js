(function(root,factory){const api=factory(root.V120_COMPONENTS);root.V120_GUIDE=api;if(typeof module!=='undefined'&&module.exports)module.exports=api;})(typeof globalThis!=='undefined'?globalThis:this,function(C){'use strict';const FLOWS=[['Crear desde cero','Home → He → prompt → IA → Resultados IA → Studio → Production → QA → Calendar → Published','factory'],['Procesar un podcast','Shim → Master Prompt → Review HTML → CONFIRMADO → manifest → Terminal/DaVinci + carruseles','shim'],['Grabar','Production → Recording → Studio Guion/Talento → Teleprompter → master/source → Editing','production'],['Resolver copies','Production → Copy → Studio Copies → manual o prompt → aprobar','production'],['Diseñar','Production → Design → Studio Diseño → prompt CON/SIN texto → Asset slot → QA','production'],['Editar','Production → Editing → Studio Edición → source/B-roll/VFX/SFX/Music → QA','production'],['Publicar','Publishing → dependencias → Calendar → PUBLISHED','calendar']];function render(){return `<section class="v120-guide">${C.sectionTitle('GUÍA','Cómo usar ABRAXAS','Rutas completas por objetivo. El Arquitecto puede acompañarte en cualquier paso.')}<div class="v120-guide-grid">${FLOWS.map(([t,d,target],i)=>`<article class="v120-panel"><span>${String(i+1).padStart(2,'0')}</span><h2>${t}</h2><p>${d}</p>${C.actionButton('Abrir ruta','nav.go',{section:target})}</article>`).join('')}</div></section>`;}return{FLOWS,render};});
