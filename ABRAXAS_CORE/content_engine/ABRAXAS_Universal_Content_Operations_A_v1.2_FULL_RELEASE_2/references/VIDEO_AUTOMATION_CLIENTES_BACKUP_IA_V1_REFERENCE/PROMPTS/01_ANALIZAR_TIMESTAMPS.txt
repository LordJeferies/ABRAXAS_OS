Lee el archivo editorial adjunto. Cada bloque representa un clip que todavía no existe: sus rangos deben extraerse del video master. Identifica clip ID, título, segmentos, orden editorial, texto, timestamps exactos/aproximados y duración. NO ordenes los segmentos cronológicamente; conserva el orden del documento. Marca `≈` como no exacto. Devuelve un manifest.json normalizado y un reporte de inconsistencias. No generes cortes nuevos ni cambies los existentes.
