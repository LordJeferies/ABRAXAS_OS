(function(root,factory){const api=factory(root.V113_DOMAIN,root.V113_STORE,root.V113_ROUTER,root.V113_COMPONENTS);root.V113_DASHBOARD=api;if(typeof module!=='undefined'&&module.exports)module.exports=api;})(typeof globalThis!=='undefined'?globalThis:this,function(DOMAIN,STORE,ROUTER,C){
'use strict';
const roleLabels={all:'My Work',strategy:'Strategy',script:'Script / Talent',copy:'Copy',recording:'Recording',design:'Design',editing:'Editing',qa:'QA',publishing:'Publishing'};
function roleSummary(role){const contents=DOMAIN.contentsForRole(role),tasks=DOMAIN.tasksForRole(role);return{contents,tasks,open:tasks.length,scheduled:contents.filter(c=>c.scheduledAt).length,ready:contents.filter(c=>['READY','READY_TO_PUBLISH','SCHEDULED'].includes(c.lifecycle)).length};}
function renderContentRow(c,role){const next=DOMAIN.nextAction(c),client=DOMAIN.clientById(c.clientId),progress=DOMAIN.productionProgress(c);return `<button class="v113-work-row" onclick="V113_ROUTER.openContent('${c.id}','${role==='all'?'overview':role}','dashboard')"><div class="v113-work-main"><div class="v113-work-tags">${C.pill(client?.name||c.clientId)}${C.pill(c.physicalType)}${C.pill(c.lifecycle,c.lifecycle==='PUBLISHED'?'success':'')}</div><b>${C.esc(c.title)}</b><small>${C.esc(c.summary||c.thesis||'')}</small></div><div class="v113-work-side"><span>${progress}%</span>${C.progress(progress)}<em>${C.esc(next.label)}</em></div></button>`;}
function renderDashboard(){const role=STORE.getState().roleMode||'all',s=roleSummary(role),all=DOMAIN.allContents(),next=[...s.contents].sort((a,b)=>(a.scheduledAt||'9999').localeCompare(b.scheduledAt||'9999')).find(c=>DOMAIN.openTasks(c).length)||s.contents[0];return `<section class="v113-dashboard"><div class="v113-dashboard-hero"><div><span class="v113-kicker">ABRAXAS · INTERFACE SYSTEM 3.0</span><h1>${role==='all'?'Tu operación editorial. Ordenada.':roleLabels[role]}</h1><p>${role==='all'?'Crea, produce, revisa y publica desde el mismo Content. Cambia de rol para ver exactamente lo que necesita cada persona.':`Vista especializada: ${roleLabels[role]}. El dominio es el mismo; solo cambia lo que necesitas ver.`}</p><div class="v113-hero-buttons">${C.button('Crear contenido',{kind:'primary',onclick:"V113_ROUTER.go('factory')"})}${C.button('Procesar una grabación',{onclick:"V113_ROUTER.go('shim')"})}${C.button('Abrir Production',{onclick:"V113_ROUTER.go('production')"})}</div></div><div class="v113-dashboard-orbit"><div><span>My Work</span><b>${s.contents.length}</b><small>contenidos visibles</small></div><div><span>Pendientes</span><b>${s.open}</b><small>tareas activas</small></div><div><span>Ready</span><b>${s.ready}</b><small>cerca de publicar</small></div></div></div><div class="v113-dashboard-grid"><article class="v113-panel v113-next-panel"><header><div><span>PRÓXIMA DECISIÓN</span><h2>${next?C.esc(next.title):'Sin bloqueos críticos'}</h2></div>${next?C.pill(DOMAIN.nextAction(next).label,'warning'):''}</header>${next?`<p>${C.esc(DOMAIN.nextAction(next).description)}</p>${C.progress(DOMAIN.productionProgress(next))}<div class="v113-card-actions">${C.button('Resolver ahora',{kind:'primary',onclick:`V113_ROUTER.openContent('${next.id}','${role==='all'?'overview':role}','dashboard')`})}</div>`:C.renderStateView('success',{title:'Todo despejado'})}</article><article class="v113-panel"><header><span>ESTA SEMANA</span><h2>${s.scheduled} calendarizados</h2></header><div class="v113-role-mini">${Object.keys(roleLabels).filter(r=>r!=='all').map(r=>{const x=roleSummary(r);return `<button onclick="V113_SHELL.setRoleModeV113('${r}')"><b>${roleLabels[r]}</b><span>${x.tasks.length}</span></button>`}).join('')}</div></article></div><section class="v113-dashboard-list"><div class="v113-list-head"><div><span>MY WORK</span><h2>${roleLabels[role]}</h2></div>${C.button('Ver Production',{onclick:"V113_ROUTER.go('production')"})}</div>${s.contents.length?s.contents.slice(0,8).map(c=>renderContentRow(c,role)).join(''):C.renderStateView('empty',{title:'No tienes trabajo pendiente en esta vista',body:'Las próximas piezas que entren a este rol aparecerán aquí.'})}</section></section>`;}
return{renderDashboard,roleSummary};
});
