# V10 Roadmap insertion

C01:
Create contracts/skeletons for:
FichaDomain, ContentImport, ContentIntent, TimeMapping, VisualMotionDomain,
MotionCaptionPolicy, ModuleGraph, CaptionCompiler, Interchange.

C02–C04:
media/scenes/transcription as planned.

C05–C06:
Pro Editor Foundation.

C07:
Content Bridge + TXT/HTML/JSON import review + provenance.

C08:
Source↔Timeline mapping / edit manifest.

C09:
Motion Context importer + first policies for Motion 00–07 and generic B-roll.

C10:
Caption Compiler reconciliation:
content roles + motion contexts + scenes + locks.

Then continue caption intelligence/render roadmap.

This bridge is implemented before advanced automation so later VAV tools can
plug in without rewriting caption logic.
