# Dockable Workspace Decision

VAV desktop must evolve as a dockable workstation.

Meaning:
- panels can be resized;
- selected panels can be detached into separate windows;
- same project state remains active;
- layouts are persisted and can be reset.

This is now a frozen direction, not an optional idea.
