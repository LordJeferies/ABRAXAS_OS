# Corrida 01 Architecture

The desktop app is React/Vite inside Tauri 2.
The local engine is a separate Node package.
Shared domain code is outside Tauri.

Preview:
Caption Plan → Remotion composition → Remotion Player.

Future export:
same composition → Remotion Renderer → FFmpeg finalization.

Transcription:
Provider Registry → Whisper.cpp default / MLX optional on Apple Silicon.
